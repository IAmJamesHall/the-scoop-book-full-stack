extends ../layout

block content
  h2= message.title
  include by_line
  p= message.body
  a(href="../") &larr; Back 
  if loggedIn
    | | 
    a(href="/messages/" + message.id + "/edit") Edit Article 
    | | 
    a(href="/messages/" + message.id + "/delete") Delete Article